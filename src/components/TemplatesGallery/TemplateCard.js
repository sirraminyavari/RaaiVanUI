import * as Styled from './TemplatesGallery.styles';

const TemplateCard = ({ index }) => {
  return <Styled.TemplateCardContainer>{index}</Styled.TemplateCardContainer>;
};

export default TemplateCard;
